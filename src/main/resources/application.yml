# default

server:
  port: 12816
  servlet:
    context-path: /api
  error:
    whitelabel:
      enabled: false

application:
  version: '1.0.4'

spring:
  profiles:
    default: dev

springdoc:
  swagger-ui:
    path: /swagger-ui.html

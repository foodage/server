# default

server:
  port: 12816
  servlet:
    context-path: /api
  error:
    whitelabel:
      enabled: false

application:
  version: ${ext.appVersion}

spring:
  profiles:
    default: local

springdoc:
  swagger-ui:
    path: /swagger-ui.html
